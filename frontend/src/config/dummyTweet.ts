/** Shown in the composer and sent to POST /post/tweet (no AI). Edit here to change the default tweet. */
export const DUMMY_TWEET_TEXT =
  "Hello from my app — posted via the API. Testing X integration without AI.";
